{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "npm install --prefix frontend && ln -sfn frontend/node_modules node_modules",
  "buildCommand": "npm run build --prefix frontend && rm -rf .next && cp -a frontend/.next .next",
  "devCommand": "npm run dev --prefix frontend"
}
