# PTA Frontend — Vercel (safe to commit as template)
# Only public URLs — auth/database credentials live on the BACKEND only.

NEXT_PUBLIC_API_URL=https://YOUR-BACKEND.vercel.app
NEXT_PUBLIC_APP_URL=https://YOUR-FRONTEND.vercel.app
